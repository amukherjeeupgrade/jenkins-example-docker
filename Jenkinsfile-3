pipeline {
  agent { dockerfile true args '--mount type=bind,source=/Users/anindamukherjee/dev/docker-mat,target=/dump'}
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          curl --version
        '''
      }
    }
  }
}
