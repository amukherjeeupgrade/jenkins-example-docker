pipeline {
  agent { dockerfile 
      {
            filename 'Dockerfile'
            additionalBuildArgs  '--build-arg version=1.0.2'
            args '-v /tmp:/tmp'
        }
    }
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          curl --version
        '''
      }
    }
  }
}
