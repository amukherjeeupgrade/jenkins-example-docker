pipeline {
  agent { 
          dockerfile 
            {
              args '--mount type=bind,source=/Users/anindamukherjee/dev/docker-mat,target=/dump', 'aninda.hprof'
             }
         }
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          curl --version
        '''
      }
    }
  }
}
