pipeline {
  agent { 
          dockerfile 
            {
              additionalBuildArgs  'aninda.hprof'
             }
         }
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          curl --version
        '''
      }
    }
  }
}
