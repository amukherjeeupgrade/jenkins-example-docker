pipeline {
  agent { dockerfile 
      {
            filename 'Dockerfile'
            additionalBuildArgs  '--build-arg version=1.0.2'
            args '--mount type=bind,source=/Users/anindamukherjee/dev/docker-mat'
        }
    }
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          curl --version
        '''
      }
    }
  }
}
